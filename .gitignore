# .gitignore for C and GNU Assembly projects

# Compiled object files
*.o
*.obj

# Executable files
# If you have a standard name for your executable, add it:
# my_program
# a.out (standard GCC output name)
# Main exec files
*/main
main
# Test exec files
*/test
test
*.out
# For Windows (if cross-compiling or developing on Windows)
*.exe
*.dll
*.pyd # Python C extensions
*.pdb # Program database files (MSVC)

# Compiled static and dynamic libraries
*.a
*.lib
*.so
*.dylib
*.bundle

# Build and output directories
# (add yours if you use different names)
build/
bin/
obj/
debug/
release/
dist/
target/
cmake-build-*/ # For CMake

# Files generated by compiler/linker
*.map      # Linker map files
*.d        # Dependency files (generated by GCC)
*.s        # Assembly files generated from C (gcc -S)
           # WARNING: if you write .s files manually, do not add this line
           # or make it more specific, e.g., build/*.s
*.lst      # Assembly listing files

# Core dump files
core
*.core

# Log files
*.log
*.log.*

# IDE and editor specific files
## Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

## CLion / JetBrains IDEs
.idea/
*.iml

## Eclipse
.metadata/
.project
.cproject
.settings/
# bin/ is already listed above, but can be Eclipse specific

## NetBeans
nbproject/
# build/ is already listed above
# dist/ is already listed above
nbdist/
.nb-gradle/

## Code::Blocks
*.cbp
*.depend
*.layout

## Sublime Text
*.sublime-project
*.sublime-workspace

## Geany
*.geany

## Vim / Neovim
*~
*.swp
*.swo
*.bak
Session.vim
.netrwhist

## Emacs
*#
.#*
*.elc

# Miscellaneous OS generated files
## macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
# ehthumbs.db (also Windows)
Icon? # Folder icon file

## Linux
*~ # Already listed for Vim, but can be generated by other programs

## Windows
Thumbs.db
ehthumbs_vista.db
desktop.ini
$RECYCLE.BIN/

# CMake files (if used)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
# Makefile (if generated by CMake; your main project Makefile should be in Git)
# If your main Makefile is named Makefile, do not add it here.
# But if CMake generates a Makefile in the build directory, this is fine.

# Other
*.stackdump # Stack dump files
*.tmp       # Temporary files
